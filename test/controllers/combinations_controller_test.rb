require 'test_helper'

class CombinationsControllerTest < ActionDispatch::IntegrationTest

  # test "should create tags after receiving HTTP request from ML service" do
  #   assert_difference 'Tag.count', 3 do
  #     post combinations_path, as: :json, params: { micropost_id: Micropost.first.id,
  #                                                  tags: ["test1", "test2", "test3"] }
  #   end
#
  #   assert_difference 'Tag.count', 1 do
  #     post combinations_path, as: :json, params: { micropost_id: Micropost.first.id,
  #                                                  tags: ["test1", "test2", "test4"] }
  #   end
  # end
#
  # test "should create combinations after receiving HTTP request from ML service" do
  #   assert_difference 'Combination.count', 3 do
  #     post combinations_path, as: :json, params: { micropost_id: Micropost.first.id,
  #                                                  tags: ["test1", "test2", "test3"] }
  #   end
#
  #   assert_difference 'Combination.count', 1 do
  #     post combinations_path, as: :json, params: { micropost_id: Micropost.first.id,
  #                                                  tags: ["test1", "test2", "test4"] }
  #   end
#
  #   assert_difference 'Combination.count', 3 do
  #     post combinations_path, as: :json, params: { micropost_id: Micropost.second.id,
  #                                                  tags: ["test1", "test2", "test3"] }
  #   end
  # end
end
